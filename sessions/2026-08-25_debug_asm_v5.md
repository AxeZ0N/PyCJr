# 2026-08-25 — debug_asm v5 decode expansion

## Verified this session

- `debug_asm` v5 emitted to `refs/pcjr_asm_debug.py` and
`mcp/pcjr_tools_server.py`.
- Selftest live via MCP: 50/50 gates pass, `ALL_PASS True`.
- C6 desync fixed; new opcodes (iret, dec, push/pop r16, A1/A3, 8B, C6)
decode correctly; truncation and OOB paths are safe; tolerant hex works.
- Legacy intact: IRPING decode_31 still 31, branch_audit passes.

## Open questions

None.

## Loose ends

- `8D` lea direction handling remains ad hoc; a single d-bit flag in
`_modrm` would simplify future additions. Not required now.

## Suggested next scope

- IVT handler work using the new opcodes (iret, 8B, C6, A1/A3) — the
handler payload this decode expansion was built for.

## Ground truth

No new anchors. No hardware run; test_log omitted.